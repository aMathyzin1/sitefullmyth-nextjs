<section class="section-card not-found">
  <h1 class="section-title">{{title}}</h1>
  <p style="color:var(--muted);">{{description}}</p>
  <a class="pill-button" href="{{actionHref}}">{{actionLabel}}</a>
</section>
